# Benchmark fixture source of truth

Files below this directory are the canonical, packaged benchmark inputs. The
repository-level `benchmarks/` tree may mirror them for development and review,
but runtime defaults resolve these resources from the installed `ooptdd` package.
