__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.tox/
.venv/
venv/
*.egg
.coverage
htmlcov/

# CI pins the independent LakatoTree judge checkout here (ci.yml lakatotree-qualification);
# ignored so the byte-exact evidence gate does not read the judge as a dirty source worktree.
_lakatotree/
